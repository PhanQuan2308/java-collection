package list.linkedList;

// + lưu trữ một danh sách liên kết các node phần tử, mỗi node sẽ lưu data và pointer
// + implement List va Deque(vì nó cung cấp cả các tính năng của một danh sách tuyến tính (list) lẫn một hàng đợi hai đầu (deque)).
// + kích thước động, các node trong danh sách liên kết không cần nằm liền kề nhau trong bộ nhớ, bởi vì mỗi node chứa một tham chiếu đến node tiếp theo.
// + Khong dong bo

public class LinkedList {
}
