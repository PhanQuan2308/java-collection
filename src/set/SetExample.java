package set;

// + Set trong Java là một Collection không cho phép các phần tử trùng lặp và lưu duoi dạng băm
// + Set không lưu trữ phần tử theo thứ tự cụ thể, ngoại trừ (LinkedHashSet, TreeSet).
// + Set không có phương thức `get(int index)` vì không lưu trữ theo chỉ số.
// + Bat dong bo


public class SetExample {
}
