package set.sortedSet;

// + La 1 interface ke thua Set,  duy trì thứ tự của các phần tử theo thứ tự tự nhiên (ví dụ, số từ nhỏ đến lớn).
// + Khong luu cac phan tu trung lap, su dung cây đỏ đen để duy trì thứ tự, cung cấp các phương thức như:
//          - first(), last(), subSet(), headSet(), và tailSet() để lấy các phần tử dựa trên thứ tự của chúng.

public class SortedSetExample {
}
